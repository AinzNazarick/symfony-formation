{% extends 'base.html.twig' %}

{% block title "Categorie : " ~ product.name %}

{%  block body %}
    <h1>{{ product.name }}</h1>
    <p>{{ product.description }}</p>
    <p>{{ product.price }}</p>
{% endblock %}